<% content_for :title do %>
    Zmień hasło
<% end %>
<h2 class="header header--main visuallyhidden">Zmień hasło</h2>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f| %>
  <%= devise_error_messages! %>
  <%= f.hidden_field :reset_password_token %>

    <fieldset>
        <legend>Zmiana hasła</legend>
        <div class="form__group required">
            <%= f.label :password, "Hasło (minimum #{@minimum_password_length.to_s} znaków)", class: 'form__label' %>
            <%= f.password_field :password, class: 'form__text', placeholder: ' ', autocomplete: 'off', autofocus: true, required: true, pattern: ".{#{@minimum_password_length.to_s},}" %>
        </div>
        <div class="form__group required">
            <%= f.label :password_confirmation, 'Potwierdź hasło', class: 'form__label' %>
            <%= f.password_field :password_confirmation, class: 'form__text', placeholder: ' ', autocomplete: 'off', required: true %>
        </div>

        <button class="form__submit" type="submit">Zapisz nowe hasło</button>
    </fieldset>
<% end %>

<%= render "devise/shared/links" %>
